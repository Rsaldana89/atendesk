# Ejemplo de variables de entorno para producción en Railway

# Puerto y host de Express (Railway establece automáticamente PORT)
PORT=3000
HOST=0.0.0.0

# Nombre del esquema en MySQL.  Usa el schema creado en Railway.
DB_NAME=railway

# Opciones de sesión seguras
TRUST_PROXY=1
COOKIE_SECURE=1

# Clave secreta de sesión (define un valor largo y aleatorio)
SESSION_SECRET=define_una_clave_secreta_larga

# Las siguientes variables no se definen aquí porque Railway las
# inyecta automáticamente al servicio Node al añadir una Variable
# Reference al servicio MySQL:
#   MYSQLHOST
#   MYSQLPORT
#   MYSQLUSER
#   MYSQLPASSWORD
#   MYSQLDATABASE

# En lugar de DB_* utiliza las referencias de Railway para host, usuario
# y contraseña, y define únicamente DB_NAME si el esquema se llama
# diferente de MYSQLDATABASE.