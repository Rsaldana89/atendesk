# Copia este archivo a .env y ajusta credenciales
PORT=3000
DB_HOST=localhost
DB_PORT=3306
DB_NAME=soportebd
DB_USER=root
DB_PASS=B1Admin
SESSION_SECRET=changeme_super_secret

# -----------------------------------------------------------------------------
# Configuración SMTP para notificaciones por correo
# Ajusta estas variables con tu proveedor (por ejemplo, Office 365).
# Si estás usando Railway o un entorno en la nube, también puedes configurarlas
# como variables de entorno en dicho servicio.
#
# SMTP_HOST   → servidor SMTP (ej. smtp.office365.com)
# SMTP_PORT   → puerto (587 para TLS STARTTLS, 465 para SSL)
# SMTP_USER   → usuario de correo (ej. tu cuenta de Office 365)
# SMTP_PASS   → contraseña o app password
# SMTP_FROM   → dirección que aparecerá en el remitente (puede ser igual al usuario)
# NOTIFY_FROM → alias opcional para el remitente de notificaciones.  Si se deja
#                en blanco, se usará SMTP_FROM.
#
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
NOTIFY_FROM=

# URL base de la aplicación utilizada para generar enlaces en los correos.
# Por ejemplo, en desarrollo http://localhost:3000 y en producción la URL
# pública asignada por Railway.
APP_BASE_URL=http://localhost:3000

# Cookie secure: pon 1 cuando sirvas la app por HTTPS; 0 en local (HTTP)
COOKIE_SECURE=0

# TRUST_PROXY indica a Express si debe confiar en los encabezados X-Forwarded-*.
# En Railway u otros proxies reversos establece esto a 1.
TRUST_PROXY=1
